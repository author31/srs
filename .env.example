DATABASE_URL=sqlite:///./flashcards.db
# TELEGRAM_BOT_TOKEN=  # Not Stored in .env, configured via dashboard
# TELEGRAM_CHAT_ID=   # Not Stored in .env, configured via dashboard
# OPENROUTER_API_KEY= # Not Stored in .env, configured via dashboard
# NOTION_API_KEY=     # Not Stored in .env, configured via dashboard
# NOTION_DATABASE_ID=  # Not Stored in .env, configured via dashboard

# Uvicorn settings
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000
UVICORN_RELOAD=False # Set to True for development, False for production
